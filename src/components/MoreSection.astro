---
---

<section class="more-section">
	<h3>And more</h3>
	<ul class="more-list">
		<li>SSH tunnels</li>
		<li>SSL connections</li>
		<li>Schema-aware autocomplete</li>
		<li>Table editor</li>
		<li>Command palette</li>
		<li>Light and dark themes</li>
		<li>Filter, sort, bulk operations</li>
		<li>OS keychain for passwords</li>
		<li>API keys stored locally</li>
		<li>No telemetry</li>
	</ul>
</section>

<style>
	.more-section {
		max-width: 1100px;
		margin: 120px auto;
		padding: 0 24px;
		text-align: center;
	}

	.more-section h3 {
		font-size: 24px;
		font-weight: 700;
		letter-spacing: -0.02em;
		margin-bottom: 12px;
	}

	.more-list {
		list-style: none;
		padding: 0;
		margin: 24px auto 0;
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: 10px 20px;
		max-width: 640px;
	}

	.more-list li {
		font-size: 13px;
		color: var(--text-muted);
	}

	.more-list li::before {
		content: "—  ";
		color: var(--border);
	}

	@media (max-width: 768px) {
		.more-section {
			margin-top: 80px;
		}

		.more-list {
			flex-direction: column;
			gap: 8px;
		}
	}
</style>
